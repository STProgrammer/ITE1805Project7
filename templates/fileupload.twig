<!DOCTYPE html>
<html>
<head>
    <title>
        Filarkiv - viser filer fra database
    </title>
    <link type="text/css" rel="stylesheet" href="stilark.css" />
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
</head>
<body>
{% include 'login2.twig' %}
    <p>Upload new file to the database: </p>
    {% if user %}
        <form enctype="multipart/form-data" method="post">
            <input type="hidden" name="MAX_FILE_SIZE" value="5365536" />
            <table border="1" cellspacing="0" cellpadding="5" width=
            "400">
                <tr>
                    <td>
                        <input name="image" type="file" />
                    </td>
                </tr>
                <tr>
                    <td>
                        <input type="checkbox" name="access" value="0">
                        Only accessible for registered users
                    </td>
                <tr><td><label>Title: </label>
                        <input type="text" name="title" required maxlength=45></td></tr>
                <tr><td><label>Description: </label>
                        <textarea type="text" name="description" rows="2" cols="20" maxlength=255 ></textarea></td>
            <tr><td><br><label>Catalog:</label>
                    <br><select name="catalogId">
                        <option value="1"> Main </option>
                        {% if catalogsList %}
                            {% for catalog in catalogsList %}
                                <option value="{{ catalog.catalogId }}">{{ catalog.catalogName }} </option>
                            {% endfor %}
                        {% endif %}
                    </select>
                        <input type="hidden" value="{{ getMac("File upload") }}" name="XSRFPreventionToken">
            <tr><td><label>Tags: </label>
                    <input type="text" name="tags" data-role="tagsinput">
                        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
                        <script src="http://bootstrap-tagsinput.github.io/bootstrap-tagsinput/dist/bootstrap-tagsinput.min.js"></script>
                    </td></tr>

            <td colspan="2" width="400" align="middle">
                <input type="submit" name="post_file" value="Post" />
            </td>
            </tr>
            </table>
        </form>
        <p>Add new catalog: </p>
        <form method="post">
            <table border="1" cellspacing="0" cellpadding="5" width="400">
                <tr>
                    <td>
                        <input type="checkbox" name="access" value="0">
                        Only accessible for registered users
                    </td>
                <tr><td><label>Catalog name: </label>
                        <input type="text" name="catalogname" required></td></tr>
                <tr><td><br><label>Catalog:</label>
                        <br><select name="catalogId">
                            <option value="1"> Main </option>
                            {% if catalogsList %}
                                {% for catalog in catalogsList %}
                                    <option value="{{ catalog.catalogId }}">{{ catalog.catalogName }} </option>
                                {% endfor %}
                            {% endif %}
                        </select>
            <input type="hidden" value="{{ getMac("Catalog upload") }}" name="XSRFPreventionToken">
            <tr><td colspan="2" width="400" align="middle">
                <input type="submit" name="post_catalog" value="Post" />
            </td>
            </tr>
            </table>
        </form>
    {% else %}
        <p>Du må logge inn for å laste opp fil</p>
    {% endif %}
</body>
</html>
