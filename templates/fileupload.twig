<!DOCTYPE html>
<html lang="en">
<head>
    <title>
        Upload file or catalog
    </title>
    <link type="text/css" rel="stylesheet" href="../stylesheet.css" />
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
</head>
<body>
{% include 'login2.twig' %}
    <p>Upload new file to the database: </p>
    {% if user %}
        <div class="form">
        <form enctype="multipart/form-data" method="post">
            <input type="hidden" name="MAX_FILE_SIZE" value="5365536" required />
            <table>
                <tr>
                    <td>
                        <input name="image" type="file" />
                    </td>
                </tr>
                <tr>
                    <td>
                        <input type="checkbox" name="access" value="0">
                        Only accessible for registered users
                    </td>
                <tr><td><label>Title: </label>
                        <br><input type="text" name="title" required maxlength=30></td></tr>
                <tr><td><label>Description: </label>
                        <br><textarea type="text" name="description" rows="2" cols="20" maxlength=255 placeholder="Max 255 characters" ></textarea></td>
            <tr><td><br><label>Catalog:</label>
                    <br><select name="catalogId">
                        <option value="1"> Main </option>
                        {% if catalogsList %}
                            {% for catalog in catalogsList %}
                                <option value="{{ catalog.catalogId }}">{{ archive.getCatalogPath(catalog.catalogId) }} </option>
                            {% endfor %}
                        {% endif %}
                    </select>
                        <input type="hidden" value="{{ getMac("File upload") }}" name="XSRFPreventionToken">
            <tr><td><label>Tags: </label>
                    <input type="text" name="tags" data-role="tagsinput" placeholder="tag1,tag2,tag3">
                        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
                        <script src="http://bootstrap-tagsinput.github.io/bootstrap-tagsinput/dist/bootstrap-tagsinput.min.js"></script>
                    </td></tr>
            <td align="middle">
                <button type="submit" name="post_file" value="Post" />Upload File</button>
            </td>
            </tr>
            </table>
        </form>
        </div>

    {% else %}
        <p>You must log in to upload new file</p>
        <br><a href="{{rel}}register/" class="button-white">Register </a>
        <a href="{{rel}}login/" class="button-white">Login page </a>
    {% endif %}
<p id="back"><a href = "../" onclick="location.href = document.referrer; return false;"><img src="{{rel}}Images/BackBtn.svg" alt="Back" width="10%"></a></p>
</body>
</html>
